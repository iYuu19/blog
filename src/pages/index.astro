---
import { getCollection } from "astro:content";
import ArchiveRow from "../components/ArchiveRow.astro";
import TagPill from "../components/TagPill.astro";
import Layout from "../layouts/Layout.astro";

const posts = (await getCollection("blog", ({ data }) => !data.draft)).sort(
  (a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf()
);

const recentPosts = posts.slice(0, 6);
const noteCandidates = posts.filter(
  (post) => post.data.difficulty === "Notes" || post.data.oj === "总结" || post.data.oj === "经验"
);
const notePosts = (noteCandidates.length > 0 ? noteCandidates : posts).slice(0, 4);
const tags = [...new Set(posts.flatMap((post) => post.data.tags))].slice(0, 12);
const latest = posts[0];
const latestDate = latest
  ? new Intl.DateTimeFormat("zh-CN", {
      year: "numeric",
      month: "2-digit",
      day: "2-digit"
    }).format(latest.data.pubDate)
  : "";
---

<Layout title="iYuu Notes" description="iYuu 的个人刷题笔记站。">
  <section class="shell intro">
    <img class="intro-avatar" src="/avatar.jpg" alt="iYuu 的头像" />
    <div class="intro-copy">
      <p class="eyebrow">iYuu Notes</p>
      <h1>菜鸟一枚，慢慢把刷题过程记下来。</h1>
      <p>
        这里主要放题解、专题总结和复盘。写下来不是为了显得厉害，而是为了以后回头看时，
        能更快找到自己真正卡过的地方。
      </p>
      <p>
        希望它最后更像一本自己的笔记，而不是一个花里胡哨的首页。
      </p>
      <div class="hero-links">
        <a href="/blog">看全部文章</a>
        <a href="/tags">看标签</a>
        <a href="/about">看看这个站在写什么</a>
      </div>
      {
        latest && (
          <p class="meta-line">
            最近更新：<a href={`/blog/${latest.slug}/`}>{latest.data.title}</a> · {latestDate}
          </p>
        )
      }
    </div>
  </section>

  <section class="shell home-layout">
    <aside class="index-column">
      <div class="site-index">
        <p class="index-title">索引</p>
        <ul class="index-list">
          <li><a href="#recent">最近更新</a></li>
          <li><a href="#notes">写这些的原因</a></li>
          <li><a href="#week">这一周</a></li>
          <li><a href="#tags">标签</a></li>
          <li><a href="#reviews">总结 / 复盘</a></li>
        </ul>
        <p class="index-meta">目前 {posts.length} 篇，最近更新在 {latestDate || "这几天"}。</p>
      </div>
    </aside>

    <div class="home-grid">
      <div class="main-column">
        <section class="section" id="recent">
          <div class="section-head">
            <h2>最近更新</h2>
            <a class="section-link" href="/blog">查看归档</a>
          </div>
          <div class="archive-list">
            {recentPosts.map((post) => <ArchiveRow post={post} />)}
          </div>
        </section>

        <section class="section" id="notes">
          <h2>我希望这里留下什么</h2>
          <ul class="plain-list">
            <li>题解尽量写清楚“为什么会想到这个思路”。</li>
            <li>不只放做对的答案，也会记第一次卡住的地方。</li>
            <li>专题总结尽量写成以后自己真的会回来看一眼的笔记。</li>
          </ul>
        </section>
      </div>

      <aside class="side-column">
      <section class="section side-note" id="week">
        <h2>这一周</h2>
        <p>
          先把站点从示例内容换成真实内容了。现在放进来的第一篇是“喵喵宠物医院”，后面再慢慢把自己的题解和复盘补进来。
        </p>
      </section>

        <section class="section" id="tags">
          <h2>标签</h2>
          <div class="tag-cloud">
            {tags.map((tag) => <TagPill label={tag} href={`/tags/${tag}/`} />)}
          </div>
        </section>

      <section class="section" id="reviews">
        <h2>总结 / 复盘</h2>
        <ul class="link-list">
          {notePosts.map((post) => (
            <li>
                <a href={`/blog/${post.slug}/`}>{post.data.title}</a>
              </li>
            ))}
          </ul>
        </section>
      </aside>
    </div>
  </section>
</Layout>
